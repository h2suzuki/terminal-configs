# Keeps the NAT mapping warm and reaps dead peers (clears stale ESTAB sessions)
ClientAliveInterval 30
ClientAliveCountMax 6

# Lets Claude use the Kitty protocol by inheriting it
AcceptEnv WT_SESSION
