# 発注書: 未記入

review-kind: none

## 目的

未記入

## スコープ

- 触ってよい path: `未記入`
- **触らない path**: 上記以外のすべて (未記入)

## 成果物

1. `未記入` の変更 (commit はしない)
2. 報告書 `未記入-report.md`

- 報告書に書くこと: 未記入
- **第一動作**: `未記入-probe.txt` を作成し 1 行書き込む
- **報告書の最終行は `REPORT_COMPLETE` の 1 行で終える**。 報告書を書いたら即座に最終出力して終了する

## 完了条件

1. 未記入

## 作業量上限 (worst-case bound)

- 変更する file は 未記入 本
- 追加する行は test を含めて 未記入 行以内
- 既存 test の削除・書き換えは 0 件 (追加のみ)
- 外部 network 取得は 0 件

## 実行してよい command

```
未記入
```

- kill-by-port は禁止する。 `fuser -k` と `pkill` は使わない。 port が塞がっていても kill せず、別 port を使うか、止められない process は放置して報告書に書く

## 適用される既存裁定 (これに反する指摘は不成立)

1. 未記入
2. commit はしない (受け入れレビューの後に発注側が行う)
3. 触らない path を越えない
4. kill-by-port (`fuser -k` / `pkill`) を使わない
5. 指示されていない挙動変更を加えない (必要なら報告書に提案として書く)
6. 判定は決定的に行い LLM 呼び出しを足さない

## 出力言語規約

- 納品物の言語は対象 file の既存規約に従う: `未記入` は 未記入 なので、それに合わせる
- 1 file の中で言語規約を混在させない。 本発注書の文体を納品物へ持ち込まない

## 報告書の書式

```markdown
# 未記入 報告書

REPORT_COMPLETE
```

## 所要見積もり

- 見積もりバンド: **未記入〜未記入 分**
- 調査発動しきい値: **未記入 分**
