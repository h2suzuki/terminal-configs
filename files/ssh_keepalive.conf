# SSH client keepalive: emit an encrypted probe every 30s on idle so the NAT keeps the mapping and
# idle sessions don't silently drop. 180s grace before giving up (tolerates transient net stalls).
Host *
    ServerAliveInterval 30
    ServerAliveCountMax 6
