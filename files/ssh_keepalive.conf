# SSH client keepalive: emit an encrypted probe every 30s on idle so the
# WSL2/Hyper-V NAT keeps the mapping and idle sessions don't silently drop.
# 30s * 6 = 180s grace before giving up (tolerates transient WSL2 net stalls).
Host *
    ServerAliveInterval 30
    ServerAliveCountMax 6
