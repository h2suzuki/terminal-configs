## Commit 自律則

commit 実行・保留・タイミング・粒度は LLM 自律で判断する。ユーザーに commit 可否を毎回聞かない。なぜなら cheap & reversible — `git reset` / `revert` / `restore` で巻き戻せるから。

## user の名称

Session の対話相手のことは、H.S. と呼称する。user と呼ばない。ただし、この固有名を永続文書に記録してはならない
