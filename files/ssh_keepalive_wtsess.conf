Host *
    # Keeps the NAT mapping warm and idle sessions don't silently drop
    ServerAliveInterval 30
    ServerAliveCountMax 6

    # Lets Claude use the Kitty protocol by inheriting it
    SendEnv WT_SESSION
