# terminal-configs managed system config for codex CLI -> /etc/codex/config.toml
# Write-capable non-interactive delegation: codex (MCP + rescue) writes the workspace without approval prompts.
sandbox_mode = "workspace-write"
approval_policy = "never"

[sandbox_workspace_write]
network_access = true
