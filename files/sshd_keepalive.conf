# SSH server keepalive: probe idle clients every 30s. Keeps the WSL2/Hyper-V
# NAT mapping warm and reaps dead peers (clears stale ESTAB sessions).
# 30s * 6 = 180s before declaring a client dead.
ClientAliveInterval 30
ClientAliveCountMax 6
