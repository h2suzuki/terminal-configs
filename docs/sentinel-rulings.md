# codex_task_sentinel 既存裁定

区分は次のとおりである。(A) は現構造の AST・文字列走査で決定的に検査できるもの、(B) は return funnel または読取経路の構造化後に機械検査できるもの、(C) は挙動 test で担保するもの、(D) はコード外のプロセス規約である。

| 番号 | 本文 | 区分 | 担保手段 |
|---:|---|:---:|---|
| 1 | **既定は cancel を指示しない**: plugin は message 本文を無加工で log へ append するため、同じ bytes が「本文が引用した偽 event」と「真正 event」の両方を表しうる。 静穏な log から stall を断定できないので、 既定は exit 14 で evidence を提示し判断を呼び手に渡す。`--trust-log` を明示した呼び手だけが exit 3 / 4 の断定に落ちる | C | `test_a_quiet_log_does_not_order_a_cancel_by_default` |
| 2 | **観測信号は sandbox 内から観測でき、 かつ当該 job に紐づくものに限る**: process liveness は pid namespace 隔離で観測不能、 broker socket も job のものと結び付かない。「pgrep で生存確認せよ」「socket を見よ」 は不成立 | D | プロセス規約 |
| 3 | **`EXIT_CONTRACT` は exit code を literal で保持する**: 定数参照にすると、 定数と契約を揃えて書き換えた変異が「同期している」と読めてしまう。 「重複だから定数を参照せよ」 は退行 | A | `test_exit_contract_codes_are_integer_literals` |
| 4 | **完了判定は「成果物 + disk 上の job record」で行う**: 「status 出力から id が消えたこと」は終了済 id が latestFinished に残るため真になり得ない | C | `test_terminal_status_with_deliverable_is_done` |
| 5 | **job id は全 workspace の state dir を走査して引く**: 隔離 worktree の job は本線 checkout から companion に尋ねると「実行中なし」と出る | C | `test_job_found_in_any_workspace` |
| 6 | **実ログ corpus test は実環境が無ければ 0 件で通る設計**: corpus は repo に入れない | D | プロセス規約 |
| 7 | **ツリーが一部でも走査できなければ `tree_age` は `None`** を返し、 exit 14 へ落とす。「読めた分だけで age を返せ」 は 17 巡目で塞いだ欠陥そのもの | C | `test_unreadable_subtree_is_not_evidence_of_quiet` |
| 8 | **走査中に消えた file は「たった今の書き込み」として扱う**: dir 一覧にあった entry が stat 時に消えていたなら、 その走査窓の中で変化が起きている。 「不可知として `None` にせよ」 は活動中ツリーを恒久的に exit 14 にするため採らない | C | `test_file_vanishing_during_the_walk_is_activity` |
| 9 | **期限は monotonic、 age と elapsed は wall clock**: 期限は経過時間なので時計合わせで動いてはならず、 mtime との差や record の UTC stamp との差は wall clock でしか意味を持たない。「全部 monotonic に統一せよ」 は不成立 | A | `test_wall_clock_call_sites_are_allowlisted` |
| 10 | **`baseline_silence` は読めない peer を飛ばす**: これは threshold の下限を上げないだけで、ツリーの状態を断定しない。 `max(STALL_FLOOR_SECONDS, ...)` の floor が残り、 その値で断定に進むのは `--trust-log` を明示した呼び手だけである | C | `test_an_uncounted_baseline_does_not_license_a_stall` |
| 11 | **成果物は strict UTF-8 で読む**: 置換して読むと binary の末尾行が token と一致して完成品に化ける。 「書きかけで例外になるから置換せよ」 は 18 巡目で塞いだ欠陥そのもの | A | `test_lossy_decode_call_sites_are_allowlisted` |
| 12 | **成果物は 3 値**: 無い / 未完成 は `False`、 在るのに読めないは `None` で exit 14 へ送る。「bool に畳め」 は completed job を「成果物なし」と誤断定する | C | `test_unreadable_deliverable_is_unknown_not_missing` |
| 13 | **消えた entry と dangling symlink は `os.lstat` で区別する**: link 先が無いだけの entry を「今の書き込み」 にすると、 その tree は永久に静穏へ到達しない | C | `test_dangling_symlink_is_not_a_write` |
| 14 | **`baseline_silence` は成長中の peer を数えない**: 「2 poll 続けて一致するまで断定を保留」案は却下済み — `--once --trust-log` が exit 4 に到達できなくなり同期 test が落ちる | C | `test_peer_growing_during_the_read_does_not_set_the_baseline` |
| 15 | **走査しきれなかった `--once` は「未登録」と言わない**: 31 巡目の修正である。「1 cadence で登録が見えなければ alive でよい」 は退行 | C | `test_once_does_not_call_an_incomplete_scan_unregistered` |
| 16 | **走査の上限に達したら結果でなく「読み切れなかった」を返す**: `MAX_SCAN_BYTES` / `MAX_TREE_ENTRIES` / `MAX_WORKSPACES` / `MAX_PEERS` は無制限 I/O で poll が期限へ戻れなくなるのを防ぐ。 「読めた分で判断せよ」 は 17 巡目で塞いだ欠陥そのものである。 advertised `st_size` は全量の証明ではない — 消費後の bounded 1 byte probe で追加 byte が返るなら、 その view は全量でない (68 巡拡張 — S_ISREG かつ size 0 の pseudo-regular を空の全量 view にしない) | C | `test_a_log_too_large_to_read_is_not_a_whole_view` / `test_a_tree_too_wide_says_unread_not_absent` / `test_workspace_names_stop_at_the_cap_before_they_are_all_read` / `test_the_peer_budget_counts_entries_not_just_records` / `test_size_zero_regular_views_are_not_whole` |
| 17 | **失敗を 1 つの番兵値に畳まない**: `record_stamp` が `("absent",)` / `("unopenable", errno)` / 実 snapshot を返し分けるのは、 別々の失敗どうしが等しくなって「不変」の証拠に化けるのを防ぐためである。 「None に統一せよ」 は退行 | C | `test_two_broken_reads_of_one_record_are_not_a_stable_corrupt` |
| 18 | **期限後に初めて見た状態から終局を返さない**: `observed_late` を跨いだ poll が返せるのは timeout だけである。 estimate も `--once` も重複判定も同じ gate を通す | A | `test_watch_returns_only_through_finish` / `test_deadline_reached_call_sites_are_allowlisted` / `test_nothing_but_a_timeout_follows_a_late_observation` |
| 19 | **完了は「成果物 + disk の record」で決まる**: log の可読性は完了条件に無い。安定して読めた completed record を log が読めないことで止めるのは退行 (34 巡目の修正) | C | `test_an_unreadable_log_does_not_block_a_completed_record` |
| 20 | **判断に使う parse と、 その安定を確かめる指紋は同じ 1 回の open から取る**:`record_view` がそれである。 外側の前後一致は中間値との一致を証明しない。 hold 済み descriptor の再読も同じ規律 — 検証済み結果を信頼して再読しないか、 再読するなら読取前後の全指紋照合を伴い、 安定した snapshot の parse 不能だけを corrupt とする (67 巡拡張 — 同 inode の truncate 窓を「安定した破損」に化けさせない) | B | `Observation` 内部は meta-test の検査対象外で、担保は挙動 test 側: `test_record_fingerprint_comes_from_the_descriptor_that_was_read` / `test_a_parse_of_other_bytes_is_not_this_polls_record` / `test_a_validated_pin_is_not_reread_during_a_truncate_window` |
| 21 | **期限を跨いだ poll が返せるのは exit 7 だけ**: contract の `7 timeout` は「no verdict by the deadline」 である。 discard・重複・estimate・`--once` のどの分岐も、 跨いだ後は終局を作らない。 これは 35 巡目の修正で 1 点に集約した。 funnel 自体の write 失敗 (EPIPE) も契約を壊さない — stdout を破棄して選択済み verdict code を返す (68 巡拡張)。 復旧手段も資源を仮定しない — sink は新規 fd 不要の in-memory stream (69 巡拡張)。 「送れない stdout」の errno は列挙しない — terminal output の全 OSError を sink 切替で守る (70 巡の EAGAIN 拡張を 73 巡で errno 非依存化: verdict は例外前に確定済みで、 errno の種類は選択済み code を捨てる根拠にならない)。 selftest / 外部 meta-test の runner stream も同じ規律 — 出力を失っても suite は完走し実合否の 0 / 1 を返す (75 巡拡張・`OSErrorStream`)。 hold 解放の close 失敗も契約を壊さない — 個別に抑止して全 handle を解放し切る (71 巡拡張・再試行はしない) | A | `test_watch_returns_only_through_finish` / `test_deadline_reached_call_sites_are_allowlisted` / `test_a_verdict_first_seen_after_the_deadline_is_a_timeout` / `test_finish_keeps_the_selected_code_when_stdout_closes` / `test_finish_needs_no_descriptor_to_recover_from_epipe` / `test_finish_keeps_the_selected_code_when_stdout_would_block` / `test_release_continues_after_a_close_oserror` / `test_finish_keeps_the_selected_code_when_stdout_is_full` / `test_selftest_runner_oserrors_preserve_the_exit_code` |
| 22 | **上限は選り分けの前に消費する**: `MAX_PEERS` は保持量でなく走査 I/O を囲う | C | `test_the_peer_budget_counts_entries_not_just_records` |
| 23 | **読めなかった候補は「id を名乗る record」 に数えない**: `carrying_records` が `carried` と `unknown` を分けて返すのはそのためで、 exit 9 は `carried` が 2 件以上の時だけ。 候補走査は descriptor を候補数に比例して保持しない — 自己誘発の fd 枯渇が重複の実在を「読めない」に変える (65 巡拡張)。 singleton の周回間比較用 hold は判定 read と指紋一致を確認した再 pin で取り、 不一致は shifting へ降格する。 同一 directory の別名 root (symlink alias) は複数 record ではない — 走査 root は到達できたものを `(st_dev, st_ino)` で重複排除し、 未作成 root は identity なしとして既存の不在分類に委ねる (71 巡拡張)。 identity と走査は前後照合で束ねる — 走査後に同じ identity を再観測できない root の結果は捨てて complete=False (72 巡拡張・A→B→A は他の path 水準観測と同じ指紋粒度の既知の残余)。 同一 directory の別名排除は workspace 層にも適用する — jobs directory identity で重複排除し、 排除集合は 1 回の走査の全 searched roots で共有する (73 巡拡張・75 巡で root 跨ぎへ一般化)。 別 directory の hardlink record は畳まない | C | `test_an_unreadable_sibling_does_not_make_a_lone_record_ambiguous` / `test_a_cross_round_duplicate_precedes_the_inode_comparison` (56 巡: 重複は inode 比較より先に exit 9) / `test_duplicate_records_do_not_exhaust_the_descriptor_budget` / `test_state_root_aliases_are_scanned_once` / `test_distinct_state_roots_still_report_ambiguity` / `test_an_uncreated_state_root_is_a_complete_absence` / `test_a_root_alias_swapped_during_its_scan_is_incomplete` / `test_workspace_aliases_are_scanned_once` / `test_workspaces_sharing_a_jobs_directory_are_scanned_once` / `test_roots_sharing_a_jobs_directory_are_scanned_once` |
| 24 | **log は完了条件ではない**: 可読性も変化も completed の先行確定を止めない。止めるのは成果物・ツリー・record 自身の変化だけである | C | `test_a_growing_log_does_not_hold_back_a_completed_record` |
| 25 | **明示された `--stall-seconds` は履歴を使わない**: 使わない材料の読み残しで判定を止めない | C | `test_an_explicit_threshold_does_not_wait_on_peer_history` |
| 26 | **record は `id` を非空 string で持つ**: 欠落・異型は「別 job を名乗った」 ではなく読めなかったのと同じ (exit 13)。 明示的に別の string を名乗るものだけが未登録側である | C | `test_record_field_of_the_wrong_type_is_unreadable` |
| 27 | **`--once` の登録側 return も期限を通す**: 1 cadence が期限を跨いだら exit 7 である。ただし期限まで待って完全な走査が空だった場合の exit 6 は、 その定義そのものなので退行ではない | A | `test_watch_returns_only_through_finish` / `test_deadline_reached_call_sites_are_allowlisted` / `test_empty_scan_deadline_exception_is_unique_and_narrow` / `test_an_incomplete_scan_at_the_deadline_is_a_timeout` / `test_registration_waits_for_the_real_deadline` |
| 28 | **`job_id` は 1 つの file 名 component**: path を渡せば `os.path.join` が先行部分を捨てる | C | `test_a_job_id_that_is_a_path_is_refused` |
| 29 | **parse に使った descriptor の指紋を、 前後の観測と一致させる**: record は `record_view`、log は `scan_log`、 peer log は `longest_silence` が返す。 外側の path 比較だけでは A→B→A が透過する | B | `Observation.record` / `Observation.log_lines` + `test_a_log_swapped_around_the_parse_is_not_one_view` |
| 30 | **貼れる cancel command を後から表示用に変換しない**: 制御文字を含む id / root では貼付可能な command を出さず、 plugin から cancel する旨を出す | C | 表示内容 = `test_a_cancel_command_is_not_offered_for_a_control_character_id` / 直接出力 site (`print` / stdout / stderr) = `test_output_call_sites_are_allowlisted` / cancel command の生成・表示経路の意味的な全数性 = レビュー観点 |
| 31 | **内容を解釈する既知の 4 経路は全て指紋を返す**: `record_view` / `scan_log` / `longest_silence` / `artifact_view` | B | 経由の構造検査 = `test_file_open_call_sites_are_allowlisted` / 指紋の実体 = `test_record_fingerprint_comes_from_the_descriptor_that_was_read` / `test_log_lines_fingerprint_comes_from_the_descriptor_that_was_read` / `test_a_peer_log_swapped_around_the_parse_is_not_history` / `test_an_artifact_swapped_around_the_read_is_not_done` / 全数性 = 構造化後も機械検査不能 (レビュー観点として残る) |
| 32 | **判定と表示は同じ値を使う**: 走査後に動いた観測から作った表示値は、 headline でも evidence でも全て「不明」 にする。 補正対象は status・root・cancel の cwd・無音・末尾・未完了 command・全量読了・履歴との倍率・baseline・threshold・ツリー・成果物である。tree と成果物は record の `workspaceRoot` / `startedAt` から引くので record の不安定も見る。 duration の表示は `duration_seconds()` に集約し、 正の値を 0 と表示しない — 契約値は round-trip 表記で同一性を保つ (66 巡拡張) | B | 表示内容 = `test_a_failed_verdict_does_not_claim_a_changed_log` 等の挙動 test 群 / 直接出力 site (`print` / stdout / stderr) = `test_output_call_sites_are_allowlisted` / 判定値と全表示値の意味的な対応の全数性 = レビュー観点 / duration 忠実性 = `test_fractional_timeout_is_preserved_in_timeout_evidence` / `test_fractional_threshold_and_integer_durations_keep_their_values` |
| 33 | **「無い」 と 「動いた」 と 「読み切れていない」 を同じ表示に畳まない**: 動いた観測を `None` や空 list や `False` に畳むと、 それぞれ「無い」 「なし」 「最後まで読めず」 という別の確定状態に化ける。 呼び手は生値と `moved` / `unscanned` の 2 集合を渡し、 表示の決定は `evidence()` に集約する。 後置 path-stat の失敗も同じ分類規律に従う — 名前消失系 (FileNotFoundError / NotADirectoryError) だけが moved で、 他の OSError は読取指紋を保持した unreadable (record は 61 巡・artifact は 65 巡で適用)。 例外 handler 内で行う再確認 syscall の失敗も同じ側 — 「読み切れなかった」 (complete=False) であって契約外の例外終了ではない (66 巡拡張)。 root の再確認は `Observation.unreachable()` で祖先の dangling symlink まで見る — 走査不能な root は完全な空走査ではない (70 巡拡張) | B | 表示内容 = `test_a_failed_verdict_does_not_claim_a_changed_deliverable` 等の挙動 test 群 / 直接出力 site (`print` / stdout / stderr) = `test_output_call_sites_are_allowlisted` / `evidence()` が担う表示決定の意味的な全数性 = レビュー観点 / stat 失敗の分類 = `test_an_unsearchable_record_ancestor_is_not_a_replacement` / `test_an_artifact_post_stat_permission_error_is_unreadable` / `test_root_recheck_permission_error_marks_the_scan_incomplete` / `test_a_root_below_a_dangling_ancestor_is_not_an_empty_scan` |
| 34 | **先行確定は完全走査を要る**: 唯一の record と示せていない poll から terminal verdict を先行確定しない | C | `test_a_record_found_behind_an_unreadable_root_is_not_taken` |
| 35 | **走査し切れていない空集合は「消えた」 でも「差し替わった」 でもない**: 候補集合の不一致を移動と読むのは完全走査の時だけ。 inode の不一致は無条件に移動である 〔後段の inode 不一致条項は裁定 61 の補完 (2026-08-21) で廃止〕 | C | 不完全走査は消失でない: `test_a_record_seen_once_is_never_called_unregistered` / `test_a_candidate_seen_in_an_incomplete_round_is_not_called_unregistered` / 不完全走査は移動でない: `test_a_record_that_changed_between_partial_scans_is_unverifiable` |
| 36 | **補正は観測ごとに閉じる**: ある観測を読み切れなかったことを、 読み切れた別の観測の理由に伝播させない | B | 表示内容 = `test_an_unreadable_peer_root_is_not_reported_as_a_change` / 直接出力 site (`print` / stdout / stderr) = `test_output_call_sites_are_allowlisted` / 観測と補正 key の意味的な対応の全数性 = レビュー観点 |
| 37 | **押さえるのは「読んだ descriptor」**: 別に開き直した fd では、 読んだ inode を押さえたことにならない。 `scan_log` / `longest_silence` は `hold` で読んだ handle を渡す。ただし record の `pinned` は「番号の再利用を防ぐ」 目的なので同一 inode の別 fd でよい | B | `Observation` の hold / release + `test_the_log_inode_is_held_while_it_is_compared` / `test_the_artifact_inode_is_held_until_the_comparison` |
| 38 | 〔裁定 61 で廃止〕 **掴めなかった理由を identity に化けさせない**: pin できなかったら、 消失は exit 10、開けないままは exit 13 をその場で返す。 同じ名前が後で読めても同じ record ではない | C | 廃止 (裁定 61) — 担保 test は撤去 round で削除 |
| 39 | 〔裁定 61 で廃止〕 **identity を番号だけで覚えない**: inode 番号は解放されると再利用される。比較の間は descriptor で inode を押さえる (record は watch 全体、 log と peer log と成果物はその比較が終わるまで)。 全量 digest は log の規模で poll I/O を倍にするため採らない | C | 廃止 (裁定 61) — 担保 test は撤去 round で削除 |
| 40 | **観測は必ず「読んだ descriptor」 から作る**: record の pin も無音も、 別に開き直したり path を stat し直したりせず、 その値を得た descriptor から作る | B | `Observation` 内部は meta-test の検査対象外で、担保は挙動 test 側: `test_record_fingerprint_comes_from_the_descriptor_that_was_read` / `test_log_lines_fingerprint_comes_from_the_descriptor_that_was_read` / `test_the_silence_comes_from_the_descriptor_that_read` |
| 41 | **観測の前後比較は、 その観測を成す全要素で行う**: ツリーは最大 mtime と entry 数、record は bytes の指紋、 log と成果物は読んだ descriptor の指紋である。 指紋は `st_ctime_ns` を含む — 同 inode・同 size・mtime 復元の書換えは ctime だけが動き、 userspace から復元できない (57 巡)。 chmod 等による false-change は安全側の保留として許容。 成果物の run への帰属も mtime 単独でなく `st_ctime_ns` ≥ startedAt を必要条件にする — 未来 mtime は utime で作れるが、 run 前の utime は ctime をその時点に進めるため弾ける (67 巡拡張) | C | `Observation` の reader 別 fingerprint + `test_a_tree_with_the_same_newest_mtime_but_other_entries_moved` / `test_a_log_swapped_around_the_parse_is_not_one_view` / `test_a_future_mtime_from_before_the_run_is_not_ready` |
| 42 | 〔裁定 62 で廃止〕 **検証していない候補を読み直して採らない**: 解決時に読めなかった候補は corrupt である。消えている場合だけ exit 10 へ委ねる。 fallback pin の名前消失例外も `record_gone()` で再分類する — dangling symlink 等の entry が残る読取不能は exit 13 で、 exit 10 は安定した消失だけ (70 巡拡張) | C | 廃止 (裁定 62) — 担保 test は撤去 round で削除 |
| 43 | 〔裁定 62 で廃止〕 **掴みは新しいものを得てから手放す**: 間を空けると番号を再利用される | B | 廃止 (裁定 62) — 担保 test は撤去 round で削除 |
| 44 | 〔裁定 61 で廃止〕 **周回の間に別 inode へ替わった record は別 record**: 同じ path でも exit 14 で返す。 比較は同一 path の単独候補どうしに限る (重複があれば裁定 23 の exit 9 が先) | C | 廃止 (裁定 61) — 担保 test は撤去 round で削除 |
| 45 | 〔裁定 61 で廃止〕 **一度見えた候補は忘れない**: 動いた候補も `seen_pairs` に残す。 忘れると完全な空走査の後に「未登録」 (exit 6) と断定できてしまう | C | 廃止 (裁定 61) — 担保 test は撤去 round で削除 |
| 46 | **読取窓の中だけ消えた観測は「無い」 ではない**: path に entry が在るのに view を読めなかったなら、 それは不在ではなく変化である。 「log なし」 「成果物なし」 は path にも無い時の答えで、不在の指紋は他の失敗と別値を名乗る。 52 巡目の修正である | C | `test_a_log_gone_only_during_the_read_is_not_stable_absence` + `test_toctou_enumerator_oracles` (log open 窓の消失 k=2 → exit 14、artifact open / moved 窓 k=4 / 9 → exit 11 の singleton assert) |
| 47 | **行の分割は LF だけで行う**: `str.splitlines()` は VT・FF・NEL・U+2028 も境界として消すので、token の後ろにそれらが付いた成果物が完成品に化ける。 「splitlines のほうが素直」 は退行。 非ゼロ offset からの読みは LF の証明を持たないので、 最初の LF までを断片として捨てる (64 巡拡張 — 左端が偶然行頭でも先頭行を採らず `whole=False` で明示する) | A | `test_splitlines_has_no_call_sites` / `test_a_scan_window_fragment_cannot_become_an_event` / `test_a_scan_window_at_a_line_start_still_drops_its_first_line` |
| 48 | **event 時刻の前後比較は数値で行い、 等値は保持する**: stamp の表現差 (小数桁の綴り) は後退ではない。 `stamp_seconds()` の返値 1 つで妥当性判定・比較・保持を行う (1 判断 1 観測)。 54 巡目の修正である | C | `test_event_lines_keep_equal_stamps_with_different_spellings` / `test_event_lines_keep_no_fraction_then_fraction_in_the_same_second` / `test_event_lines_drop_a_fractional_step_backwards` / `test_event_lines_drop_no_fraction_after_a_fraction` |
| 49 | **明示された空の `--state-root` は usage error**: 空文字を「未指定」 に畳んで既定 root 群へ検索を広げない。 直接呼び出しの `state_roots("")` も `[""]` を返し、 呼び手の指定範囲を勝手に広げない。 54 巡目の修正である | C | `test_an_empty_state_root_is_a_usage_error` / `test_an_explicit_empty_state_root_stays_explicit` |
| 50 | **時刻の前後比較は受理する全精度で exact に行う**: epoch を binary64 に落とすと 2026 年付近の刻みは約 238 ns で、 それ未満の後退が等値に化ける (偽終了行の通過・stale 成果物の fresh 誤認)。 stamp は exact な十進値で 1 回だけ parse して順序判定は exact のまま行い、 間隔などの秒数だけを float 化する。 成果物側は `st_mtime_ns` を同じ exact 領域で比較する。 裁定 48 の「数値」 の精度をここまで含めて読む。 55 巡目の修正である | C | `test_a_one_nanosecond_older_fake_end_does_not_finish_a_command` / `test_an_artifact_one_nanosecond_before_the_run_is_stale` (utime 後の `st_mtime_ns` 保持を assert — 56 巡) / `test_pre_epoch_fractions_move_forward_and_keep_artifacts_stale` (負 epoch は fraction を数値加算 — 56 巡) |
| 51 | **`isdigit()` は `int()` 可を意味しない**: 非 ASCII digit (superscript 等) は `isdigit()` 真でも `int()` が ValueError を出す。 数値化する component は `isascii() and isdigit()` で限定する。 同じ受理域クラスの全 site (時刻 regex は `re.ASCII`、 CLI 数値引数は ASCII 検査) に適用する。 55〜56 巡の修正である | C | `test_a_non_ascii_digit_version_does_not_break_companion_selection` / `test_a_fullwidth_year_cannot_finish_a_pending_command` / `test_a_fullwidth_record_stamp_is_corrupt` |
| 52 | **標準外の record JSON は corrupt**: `json.loads` の後勝ちは曖昧な record を確定値に化けさせるので `object_pairs_hook` で重複名を ValueError に (57 巡)、 既定 decoder が受理する `NaN` / `Infinity` / `-Infinity` も `parse_constant` で ValueError にし (65 巡拡張)、 いずれも既存 corrupt 経路へ送る | C | `test_duplicate_record_fields_are_corrupt` / `test_non_standard_json_constants_are_corrupt` |
| 53 | **出力 funnel は stream encoding を仮定しない**: ASCII stdout で日本語 evidence が UnicodeEncodeError になると契約外の例外終了になる。 `main()` が stdout / stderr を `errors="backslashreplace"` に reconfigure する (無い stream は skip)。 57 巡目の修正である | C | `test_ascii_stdout_preserves_the_terminal_exit_and_full_evidence` |
| 54 | **stamp の小数部は 9 桁 (ns) を上限に受理する**: runner は 3 桁固定を書き、 比較相手の `st_mtime_ns` は 9 桁が最大分解能。 超過は LOG_TS では event でない本文、 RECORD_TS では corrupt。 無制限受理が binary64 (r55) と Decimal context (r58) の深掘り供給源だったため、 クラスを仕様レベルで閉じる司令塔裁定である | C | `test_a_ten_digit_fake_end_does_not_finish_a_command` / `test_a_ten_digit_record_stamp_is_corrupt` |
| 55 | **列挙の上限は件数と保持 byte の両輪で囲う**: tree 走査は `MAX_TREE_ENTRIES` に加え、 保持する path の累積 byte を `MAX_TREE_PATH_BYTES` で抑え、 超過は「読み切れなかった」 を返す。 companion 候補も `MAX_COMPANION_CANDIDATES` 内で逐次評価し、 超過は部分集合から選ばず fallback へ落とす。 cap は保持候補でなく filter 前の raw scandir entry を消費する (63 巡拡張 — 非一致 entry だけの無制限走査を防ぐ)。 列挙の途中失敗も部分集合から選ばず fallback へ、 cadence を跨ぐ候補履歴も `MAX_SEEN_PAIRS` 内で、 到達時は忘れて続行でなく解決不能の契約 exit (64 巡拡張) 〔候補履歴 cap 条項は裁定 61 の補完 (2026-08-21) で廃止〕。 「件数 cap が byte も囲う」「plugin cache の版数は常に少ない」 という未検証前提が MemoryError の例外終了経路だった。 62 巡目の修正である | C | `test_tree_path_bytes_are_bounded_before_the_entry_cap` / `test_tree_path_bytes_within_the_cap_keep_the_walk_result` / `test_too_many_companion_candidates_use_the_fallback` / `test_nonmatching_companion_entries_consume_the_scan_cap` / `test_companion_candidates_within_the_cap_keep_numeric_order` / `test_companion_iteration_failure_discards_the_partial_best` |
| 56 | **companion 版名は semver-lite で順序付ける**: 各 dot component は (先頭の連続 ASCII 数字による数値 prefix, suffix なしか) の組で比較し、 数値が大きい方が勝ち、 同値なら suffix なし (release) が suffix 付き (prerelease) に勝つ。 `1.0.10-beta` は `1.0.9` に勝ち `1.0.10` に負ける。 component 丸ごとの `isdigit` 判定は suffix 付き版を 0 に畳み、 古い版の cancel command を evidence に出す。 64 巡目の司令塔裁定である | C | `test_suffix_versions_follow_semver_lite_order` |
| 57 | **`.git` は tree activity から設計上除外する**: 標準運用の worktree では `.git` は gitdir を指す file で git 状態の実体は watched tree の外にあり、 full-checkout root では checkout-local な git 背景活動 (gc・index refresh 等) が job と無関係に `.git` を書いて settling を永遠に妨げる。 完了の一次権威は成果物 + record (裁定 4 / 19) で tree は補助信号。 「`.git` も測れ」 は completion livelock 側の退行。 68 巡目の司令塔裁定である | C | `test_tree_age_skips_git_and_reports_newest` |
| 58 | **成果物の run 帰属は POSIX metadata を必要条件として判定し、 敵対的 local actor は脅威 model 外**: `st_mtime_ns`・`st_ctime_ns` ≥ startedAt (Decimal exact) は帰属の必要条件であって content provenance の十分条件ではない — chmod 等の metadata-only 操作で ctime は本文を変えず前進できる。 metadata を意図的に偽装する local actor への防御は、 record 側に帰属情報が無い限り filesystem 観測では不可能であり要求しない (裁定 2 の観測可能性と同系)。 content fingerprint 基準化は「完了後に監視を張り直す再 arm」を帰属不能に落とすため不採用。 69 巡目の司令塔裁定である | D | プロセス規約 (脅威 model の境界) |
| 59 | **reader の 1 syscall は有限時間で返る filesystem を実行環境の前提とする**: 監視対象 (plugin state dir・log・artifact) は local filesystem 運用であり、 無応答の network / FUSE filesystem 上の regular file read が deadline を越える class は要求しない。 O_NONBLOCK は regular file の read latency を囲えず (open(2): regular file に効果なし)、 真の deadline 保証は per-read の process 隔離を要する — 監視対象が同じ fs 上にある本 tool では不成立。 O_NONBLOCK open は special file の open 停止回避のためで、 S_ISREG 確認後の blocking 化は維持する。 72 巡目の司令塔裁定である | D | プロセス規約 (実行環境の境界) |
| 60 | **指摘の妥当性はユースケース正本からの逸脱度で判定する**: `docs/sentinel-use-cases.md` が用途と環境前提の正本である。 指摘は U0 (ユースケース内で発生) / U1 (境界 — 過去裁定と正本で照合し、 決定不能なら人間に確認してよい) / U2 (目的外利用 = invalid・バグではない) に分類し、 blocking は U0 のみ。 用途を広げる要求は欠陥報告ではない — 普段使いの鋏に手術用の要件を課すのは目的外である。 逆向きも同じ — 正本に無い状況のためだけの実装・複雑さは過剰実装として U0 の欠陥である (cost が用途に合わない作りは保守を重くする)。 ただし削減が既存裁定の担保と衝突する場合は裁定優先で、 裁定の変更は人間へ。 74 巡後にユーザー承認で導入した司令塔裁定である | D | プロセス規約 (`docs/sentinel-use-cases.md` と対) |
| 61 | **同 path 別 inode 差し替えの防御は撤去する**: 裁定 38 / 39 / 44 / 45 が定めた「同じ path が別 inode に替わった」ことの検出・防御 (pin の identity 化・descriptor による inode 保持・周回間 inode 比較・seen_pairs の記憶) は、 companion だけが書く plugin state dir という用途正本の運用に無い状況のための複雑さであり、 裁定 60 の過剰実装 = U0 に該当する。 意図的に inode を偽装する local actor は裁定 58 と同じく脅威 model 外。 裁定 38 / 39 / 44 / 45 を廃止する。 撤去は専用 round で実施し、 確認巡 r77 で検証する。 2026-08-13 の U1 決裁 (a) を 2026-08-21 に ledger-first で記録した司令塔裁定である。 **廃止列挙の補完 (2026-08-21 ユーザー承認)**: 裁定 35 の「inode の不一致は無条件に移動」条項と、 裁定 55 の「cadence を跨ぐ候補履歴 cap (`MAX_SEEN_PAIRS`)」条項も同じ防御 family として廃止する (r77 の上流批評で列挙漏れを検出) | D | プロセス規約 (撤去 round + 確認巡の敵対レビューで検証) |
| 62 | **解決時に読めなかった候補は、次の cadence で読み直し、安定した snapshot で再判定する**: 裁定 42 (読めなかった候補の恒久不採用) と裁定 43 (inode 再利用を理由とする hold 置換順) を廃止する。 record の同 path 別内容は、悪意ある差し替えでなく producer 自身の上書き (S-9 の writeFileSync) だけがユースケース上のシナリオである (裁定 58 の脅威 model 外・裁定 61 の防御撤去と同根。 2026-08-21 ユーザー確認「発注は独立 workspace で隔離されており、悪意がなければすり替えは起きえない — あり得ないことに対処するコードは廃止」)。 読み損ねの恒久 latch は正常な上書き窓を見張り実行 1 回分の誤報に固定するため撤去し、 corrupt の確定は裁定 20 の「安定した snapshot の parse 不能」に一本化する。 読み損ねた cadence は解決未了として続行し、 安定した消失は exit 10、 期限は既存の timeout 契約に委ねる | D | `test_an_unreadable_candidate_is_retried_on_the_next_cadence` / `test_changing_candidates_keep_retrying_until_timeout` + プロセス規約 (確認巡で検証) |
